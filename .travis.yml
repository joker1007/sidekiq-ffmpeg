language: ruby
cache:
  - bundler
  - apt
before_install:
  - sudo add-apt-repository -y ppa:samrog131/ppa
  - sudo apt-get update -q
  - sudo apt-get -y install ffmpeg
rvm:
  - 2.1.1
notifications:
  email:
    on_success: change
